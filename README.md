# CVFS-Customised-Virtual-File-System-
Customised Virtual File System (CVFS) is a user-space file system simulation that models core operating system file management concepts. It uses inodes, file tables, and dynamic memory to support basic file operations and demonstrates how files are managed internally by an OS.
